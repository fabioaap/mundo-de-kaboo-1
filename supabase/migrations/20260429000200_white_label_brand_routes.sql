-- Placeholder migration to represent remote migration history.
-- Remote schema_migrations contains: 20260429000200 white_label_brand_routes
-- This repository currently lacks the original SQL body that was applied remotely.
-- The remote database already contains public.brand_routes and related route data.
-- DO NOT assume this placeholder recreates the remote schema; it only records the missing version ID locally.
--
-- Follow-up required before any mutable remote action:
-- 1. Reconstruct the historical SQL for brand_routes if available.
-- 2. Confirm table shape/RLS/indexes against the remote database.
-- 3. Replace this placeholder only when the reconstructed SQL is trustworthy.

-- no-op on purpose
SELECT 1;
